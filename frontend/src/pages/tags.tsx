import { api } from "@/services/api"; import { RecordsPage } from "@/components/RecordsPage"; export default function Tags(){ return <RecordsPage title="Tags" eyebrow="Tracking hardware" load={api.tags.list} columns={[{label:"Tag ID",key:"tag_id"},{label:"Type",key:"tag_type"},{label:"Assigned asset",key:"asset_id"},{label:"Battery",key:"battery_level"},{label:"Status",key:"status"}]}/>; }
