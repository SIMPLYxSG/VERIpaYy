import { api } from "@/services/api"; import { RecordsPage } from "@/components/RecordsPage"; export default function Employees(){ return <RecordsPage title="Employees" eyebrow="Ownership & allocation" load={api.employees.list} columns={[{label:"Employee",key:"employee_name"},{label:"Department",key:"department_name"},{label:"Email",key:"email"},{label:"Status",key:"status"}]}/>; }
