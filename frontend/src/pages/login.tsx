import { useRouter } from "next/router";
export default function Login() { const router = useRouter(); return <main className="grid min-h-screen place-items-center bg-background p-5"><section className="card w-full max-w-md rounded-2xl p-8"><div className="mb-8 flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary font-bold text-surface">A</span><div><h1 className="font-semibold">AssetTrack</h1><p className="text-sm text-muted">Secure operations access</p></div></div><h2 className="text-2xl font-semibold">Welcome back</h2><p className="mt-2 text-sm text-muted">Sign in to manage your physical asset estate.</p><form className="mt-7 space-y-4" onSubmit={e => { e.preventDefault(); router.push("/dashboard"); }}><label className="block text-sm font-medium">Work email<input required type="email" className="mt-1.5 w-full rounded-lg border border-line bg-surface px-3 py-2.5" placeholder="you@company.com" /></label><label className="block text-sm font-medium">Password<input required type="password" className="mt-1.5 w-full rounded-lg border border-line bg-surface px-3 py-2.5" /></label><button className="w-full rounded-lg bg-primary py-2.5 font-semibold text-surface">Sign in</button></form></section></main>; }
