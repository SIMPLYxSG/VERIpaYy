import { AppLayout } from "@/layouts/AppLayout"; export default function Settings(){return <AppLayout><p className="label">Workspace administration</p><h1 className="mt-1 text-3xl font-semibold">Settings</h1><section className="card mt-6 rounded-xl p-6"><h2 className="font-semibold">Organization settings</h2><p className="mt-2 text-sm text-muted">Configure integrations, alert defaults, and access controls here.</p></section></AppLayout>}
