import { FormEvent, useState } from "react"; import { Bot, Send } from "lucide-react"; import { AppLayout } from "@/layouts/AppLayout"; import { api } from "@/services/api";
export default function Assistant(){const [query,setQuery]=useState("");const [answer,setAnswer]=useState("");const [loading,setLoading]=useState(false);const send=async(e:FormEvent)=>{e.preventDefault();if(!query)return;setLoading(true);try{setAnswer((await api.assistant(query)).answer)}finally{setLoading(false)}};return <AppLayout><div className="mx-auto max-w-3xl"><div><p className="label">Controlled operations assistant</p><h1 className="mt-1 text-3xl font-semibold">AI Assistant</h1><p className="mt-2 text-muted">Ask questions using approved AssetTrack backend tools.</p></div><section className="card mt-6 rounded-xl p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary text-surface"><Bot size={21}/></span><div><h2 className="font-semibold">AssetTrack Assistant</h2><p className="text-sm text-muted">Answers are generated from controlled API access.</p></div></div>{answer&&<div className="mt-6 rounded-xl bg-background p-4 text-sm leading-6">{answer}</div>}<form onSubmit={send} className="mt-6 flex gap-2"><input value={query} onChange={e=>setQuery(e.target.value)} className="min-w-0 flex-1 rounded-lg border border-line bg-surface px-3 py-2.5" placeholder="Where is LAP-028?"/><button disabled={loading} className="rounded-lg bg-primary px-4 text-surface" aria-label="Send question"><Send size={18}/></button></form><div className="mt-5 flex flex-wrap gap-2">{["Where is LAP-028?","Which chairs moved today?","Which assets have not been seen for 24 hours?","Show unusual movements this week."].map(q=><button key={q} onClick={()=>setQuery(q)} className="rounded-full border border-line px-3 py-1.5 text-xs text-muted hover:border-secondary">{q}</button>)}</div></section></div></AppLayout>}
