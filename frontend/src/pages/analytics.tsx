import { AppLayout } from "@/layouts/AppLayout"; export default function Analytics(){return <AppLayout><p className="label">Insights</p><h1 className="mt-1 text-3xl font-semibold">Analytics</h1><section className="card mt-6 rounded-xl p-6"><h2 className="font-semibold">Movement & utilization intelligence</h2><p className="mt-2 max-w-xl text-sm text-muted">Analytics will use historical location readings and permanent movement records once the backend data pipeline is connected.</p></section></AppLayout>}
