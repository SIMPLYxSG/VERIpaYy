export function LoadingState() { return <div className="grid gap-4 sm:grid-cols-3">{[1,2,3].map(i => <div key={i} className="h-28 animate-pulse rounded-xl bg-sage/30" />)}</div>; }
export function ErrorState({ retry }: { retry: () => void }) { return <div className="card rounded-xl p-8 text-center"><h2 className="text-lg font-semibold">We couldn’t load this data</h2><p className="mt-2 text-sm text-muted">Check your connection to the AssetTrack API and try again.</p><button onClick={retry} className="mt-5 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-surface">Retry</button></div>; }
export function EmptyState({ title, text }: { title: string; text: string }) { return <div className="card rounded-xl p-10 text-center"><h2 className="font-semibold">{title}</h2><p className="mt-2 text-sm text-muted">{text}</p></div>; }
